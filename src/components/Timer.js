import './../css/Timer.css';
// import { useState } from 'react';

export default function Timer(props) {
  /* const incrementRate = 365 / props.eventsCount;
  const [passed, setPassed] = useState(0);
  const [incrementTimer, setIncrementTimer] = useState(true);
  
  console.log('rebuilt');
  
   if(incrementTimer) {
     setIncrementTimer(false);
     setPassed(passed + incrementRate);
   } */
   
  return (
    <div className="timer">
      {/*passed*/} zile au trecut
    </div>
  );
}